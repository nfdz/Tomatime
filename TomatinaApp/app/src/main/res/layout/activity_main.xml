<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <android.support.v4.view.ViewPager
        android:id="@+id/main_activity_vp"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1" />

    <android.support.design.widget.TabLayout
        android:id="@+id/main_activity_tl"
        android:layout_width="match_parent"
        android:layout_height="@dimen/main_activity_tabs_height"
        app:tabIndicatorColor="@color/colorAccent"
        app:tabIndicatorHeight="@dimen/main_activity_tabs_indicator_height"
        app:tabBackground="@color/tabBackground"
        app:elevation="0dp"
        android:hapticFeedbackEnabled="true"
        app:tabTextColor="@color/textColorDark"
        app:tabSelectedTextColor="@color/textColorDark"
        app:tabTextAppearance="@style/MainTabText" />

</LinearLayout>